language: python
addons:
  apt:
    packages:
      - shellcheck
      # shfmt is already available
script:
  - make check
  - make test-bash
  - make test-regex

  # The repository must remain unchanged
  - git diff-index --exit-code HEAD
